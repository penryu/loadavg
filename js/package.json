{
  "name": "loadavg",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild": "tslint -p tsconfig.json -c tslint.json",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/loadavg.js",
    "watch": "nodemon dist/loadavg.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.12.18",
    "ts-node": "^8.0.1",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4"
  }
}
