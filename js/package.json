{
  "name": "loadavg",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "the-private-item": "webpack-only?",
  "scripts": {
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "prewatch": "npm run lint",
    "watch": "tsc -w",
    "prestart": "npm run lint",
    "start": "tsnd --no-notify --respawn --transpileOnly src/index.ts",
    "prebuild": "npm run lint",
    "build": "webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.12.18",
    "node-dev": "^3.1.3",
    "ts-loader": "^5.3.3",
    "ts-node": "^8.0.1",
    "ts-node-dev": "^1.0.0-pre.32",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1"
  }
}
